
/* 
api/url là tên gọi của đường link 
gọi api/url sau đó nhận được data, và hành này được gọi là request
bên phía client thực hiện request (gọi api) -> phía server trả về data 
gọi api/url về bản chất chính là thông báo lên server và server phản hồi về data 
mô hình : request -> data ( kèm theo trạng thái của request )
*/

// Các trạng thái của request -> search : status code http 
// Dựa vào các status code để có thể biết được trạng thái sau khi request

